#!/usr/bin/env bash

# this pack is valid for apps with a hello.txt in the root
if [ -f $1/app.psgi ]; then
  echo "Perl/PSGI"
  exit 0
elif [ -f $1/META.info ] || [ -f $1/.perl6-version ]; then
  echo "Perl6/META.info"
  exit 0
else
  exit 1
fi
